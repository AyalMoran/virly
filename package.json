{
  "name": "virly",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "npm run dev --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "storybook": "npm run storybook --workspace client",
    "build-storybook": "npm run build-storybook --workspace client",
    "test:client": "tsx --tsconfig client/tsconfig.json --test \"client/tests/**/*.test.tsx\"",
    "test:server": "tsx --tsconfig server/tsconfig.json --test \"server/tests/**/*.test.ts\"",

    "seed:personal-details": "node scripts/fill-personal-details.api.js",
    "seed:transactions": "bash scripts/seed-transactions.sh",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "docs:api": "npx @redocly/cli build-docs openapi.yaml --output docs/api/index.html"
  },
  "dependencies": {
    "@langchain/core": "^1.1.48",
    "@langchain/langgraph": "^1.4.2",
    "@langchain/openai": "^1.4.7",
    "zod": "^4.4.3"
  }
}
