node_modules
dist
.env*
.DS_Store
coverage

# Build artifacts (Storybook build output, TS incremental cache, compiled vite config)
client/storybook-static/
*.tsbuildinfo
*.log
client/vite.config.js
client/vite.config.d.ts

# Generated ML artifacts (scaler/model weights are fit from the user's dataset)
server/artifacts/

# Generated Graphify artifacts (graphify is a CLI tool for generating graph data about the codebase)
graphify*

# docs/ is tracked, but keep transient agent run-logs and scratch playgrounds out
docs/**/*.log

.agents
.claude
.superpowers
.instance
.vscode

/skills-lock.json

# LangGraph API
.langgraph_api


# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync
.design-sync/.cache/
.design-sync/learnings/
.design-sync/previews/
.design-sync/node_modules
client/.ds-styles.css
